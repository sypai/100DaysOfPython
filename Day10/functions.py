# Format a name
def format_name(f_name, l_name):
    """This function takes as input first name anad last name
    and returns the title case version of the name. """
    return f_name.title() + ' ' + l_name.title()

print(format_name("Suyash", "bAjpai"))

